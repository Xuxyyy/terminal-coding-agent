# Identifier helper

Run `npm run check` after changing identifier normalization.

The helper turns a label into a lowercase identifier. Leading and trailing
space is removed, and every run of non-alphanumeric characters becomes one
hyphen.

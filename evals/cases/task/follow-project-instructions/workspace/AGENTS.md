# Repository instructions

For work on the title normalizer:

- Edit only `src/title.js`.
- Preserve the named export `normalizeTitle`.
- Import and use `WORD_SEPARATOR` from `./constants.js`; do not duplicate the separator as a string literal in `src/title.js`.
- Do not edit tests, constants, package metadata, or this instruction file.
- Verify the result with `npm test`.

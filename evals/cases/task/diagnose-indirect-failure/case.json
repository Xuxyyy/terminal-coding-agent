{
  "id": "diagnose-indirect-failure",
  "suite": "focused",
  "category": "edit",
  "task": {
    "prompt": "The request retry test is failing. Find the root cause and fix the implementation without changing the tests. Run the tests when you are done.",
    "mode": "auto-edits",
    "policy": "yes",
    "maxSeconds": 120
  },
  "grade": {
    "allowedWrites": [
      "src/retry-delay.js"
    ],
    "checks": [
      {
        "kind": "exit0",
        "command": "node --test"
      },
      {
        "kind": "exit0",
        "command": "node --input-type=module -e \"import {retryDelay} from './src/retry-delay.js'; const expected = [100, 200, 400, 800, 1600]; for (let index = 0; index < expected.length; index += 1) { const attempt = index + 1; const actual = retryDelay(attempt); if (actual !== expected[index]) { console.error('attempt ' + attempt + ': expected ' + expected[index] + ', got ' + actual); process.exit(1); } }\""
      },
      {
        "kind": "unchanged",
        "path": "test/request.test.js"
      }
    ]
  }
}

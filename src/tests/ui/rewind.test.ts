import test from 'node:test';

test('the picker lists one row per user message', {todo: 'v4 step 6'});

test('the newest checkpoint is selected first', {todo: 'v4 step 6'});

test('a long message is truncated to the width', {todo: 'v4 step 6'});

test('an empty conversation offers nothing to rewind', {todo: 'v4 step 6'});

test('cancelling the picker changes nothing', {todo: 'v4 step 6'});
